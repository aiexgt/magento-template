type CustomResponse {
    message: String!
    status: Boolean!
}

type Query {
    customQuery(param: String!): CustomResponse @resolver(class: "Vendor\\GraphQL\\Model\\Resolver\\CustomQuery")
}

type Mutation {
    customMutation(input: String!): CustomResponse @resolver(class: "Vendor\\GraphQL\\Model\\Resolver\\CustomMutation")
}